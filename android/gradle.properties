# Gradle configuration
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Keystore configuration (update the following with your actual passwords and paths)
KEYSTORE_PASSWORD=rudra@123          # Keystore password
KEY_ALIAS=androiddebugkey            # The alias name of your key
KEY_PASSWORD=rudra@123               # Key password (same as keystore password for default setup)
